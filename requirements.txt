--extra-index-url https://download.pytorch.org/whl/cpu

# --- FIX PARA HTTPX (COMPATIBILIDAD GENAI) ---
httpx>=0.28.1,<1.0.0
httpcore>=1.0.5,<1.1.0
# -----------------------------------------

# --- FIX PARA NUMPY 2.0 (Evita conflictos de tensores) ---
numpy==1.26.4

# --- TORCH CPU (Sin CUDA) ---
torch==2.2.2+cpu
torchvision==0.17.2+cpu

# --- MODELO 
transformers==4.45.0
accelerate==0.34.0

# --- MODELO LOCAL (YA NO SE USA - ELIMINADO)
# llama-cpp-python==0.2.77  # Eliminado: ahora usamos multi-provider cloud (Groq, GitHub, HF)

# --- LLAMAINDEX (ACTUALIZADO: Versiones que instalaste manualmente) ---
# Usamos >= para permitir futuras mejoras sin romper todo
llama-index-core>=0.14.0  
llama-index-embeddings-huggingface>=0.6.0
llama-index-llms-ollama>=0.9.0
llama-index-llms-huggingface>=0.6.0

# --- CORE ---
sentence-transformers==3.0.1
fastapi==0.111.0
uvicorn==0.30.1
jinja2==3.1.4
requests==2.32.3
python-dotenv==1.0.1
pdfplumber==0.11.0

# --- HERRAMIENTAS ---
fuzzywuzzy==0.18.0
python-levenshtein==0.25.1
pandas==2.2.2
biopython==1.81
rank-bm25>=0.2.2

# --- BASES DE DATOS ---
pymongo==4.8.0
chromadb==0.4.24
pymilvus==2.4.4

# --- OTROS ---
reportlab==4.2.2
plotly==5.22.0
semanticscholar==0.9.0
habanero==1.2.0
openai==1.35.3
marshmallow==3.21.3
environs==9.5.0
matplotlib>=3.8
fpdf2>=2.7.9
pillow>=10.0
google-genai>=1.52.0